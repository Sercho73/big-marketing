---
interface Props { direction?: 'up' | 'right'; }
const { direction = 'up' } = Astro.props;
---
<svg class:list={['arrow', { 'arrow-right': direction === 'right' }]} viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M5 19 19 5M5 5h14v14" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" /></svg>
